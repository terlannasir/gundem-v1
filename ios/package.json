{
  "name": "gundem-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Gündəm — iOS app (Capacitor). Loads the web app from the server (server.url), so UI updates need no new build.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "8.5.2",
    "@capacitor/ios": "8.5.2",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capgo/capacitor-native-biometric": "^8.6.11"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
